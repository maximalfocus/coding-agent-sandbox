# Keep the build context small and free of secrets / your project files.
.git
workspace/
.env
.devcontainer/
*.md
run.sh
run.ps1
shell.sh
shell.ps1
scripts/
# …except the two scripts baked INTO the image (see Dockerfile): the shared skills linker and the
# first-run reminder hint. Re-included from the otherwise host-only scripts/ tree.
!scripts/skills/link-skills.sh
!scripts/skills/sandbox-todo-hint.sh
