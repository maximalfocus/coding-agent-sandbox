# OPT-IN AWS IAM Identity Center state for the content-mediation stack.
services:
  claude-sandbox-mitm:
    volumes:
      - "claude-aws:/home/node/.aws"

volumes:
  claude-aws:
    name: "${SANDBOX_AWS_VOLUME_NAME:-coding-agent-sandbox-aws}"
